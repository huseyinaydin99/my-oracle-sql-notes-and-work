--EQUI JOINS
SELECT
E1.ENAME, D1.DEPTNO
FROM EMP E1, DEPT D1
WHERE E1.DEPTNO = D1.DEPTNO;

--OUTER JOIN(LEFT JOIN)
--(+) KULLANIMINA ORNEK:
SELECT
E1.ENAME, D1.DEPTNO
FROM EMP E1, DEPT D1
WHERE E1.DEPTNO(+) = D1.DEPTNO; --BENCE + KISMONDA YANLISLIK VAR.! SOLA KOYULURSA RIGHT JOIN, SAGA KOYULURSA LRFT JOIN (:

--OUTER JOIN(RIGHT JOIN)
--(+) KULLANIMINA ORNEK:
SELECT
EMPNO, ENAME, D1.DEPTNO, LOC
FROM EMP E1, DEPT D1
WHERE E1.DEPTNO = D1.DEPTNO(+);


--ANSI FORMATINDA JOINLEMELER
--INNER JOIN
SELECT
EMPNO, ENAME, D1.DEPTNO, LOC
FROM EMP E1
INNER JOIN DEPT D1
ON E1.DEPTNO = D1.DEPTNO;

--SADECE JOIN(INNER JOIN ILE BIREBIR AYNI SEYDIR.!)
SELECT
EMPNO, ENAME, D1.DEPTNO, LOC
FROM EMP E1
JOIN DEPT D1
ON E1.DEPTNO = D1.DEPTNO;
--ON E1.X = D1.Y; --X ILE Y KOLONLARI ILISKISI
--BIRDEN FAZLA KOLONLU ILISKI KURMAK ICINDIR.!












--



















-